import styled from "styled-components";

export const TrashWrapperStyled = styled.div`
  margin-left: ${(props) => props.theme.panelsSize.sideBarSize};
  margin-top: ${(props) => props.theme.panelsSize.headerSize};
  padding-left: 30px;
`;
